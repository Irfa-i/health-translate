# Healthcare Translation — Prototype (Free)

This prototype uses:
- Browser Web Speech API (speech -> text)
- googletrans (free) for translation on backend
- Browser SpeechSynthesis for audio playback

## Setup (Windows PowerShell)
1. Create venv (if not created yet):
   ```powershell
   python -m venv venv
